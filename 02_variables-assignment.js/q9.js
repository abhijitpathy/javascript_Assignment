
// Changed 'x' to 'count' and initialized it with a meaningful default value.
let count = 0;

// Split 'a', 'b', and 'c' into separate lines and gave them meaningful names.
let firstNumber = 1;
let secondNumber = 2;
let thirdNumber = 3;

// Changed 'pi' to 'PI' and 'let' to 'const' because its value should not change.
const PI = 3.14159;

// Renamed 'username' to 'userName' using camelCase.
let userName = "John";

// Renamed 'itemcount' to 'itemCount' using camelCase.
let itemCount = 0;

console.log(count);
console.log(firstNumber);
console.log(secondNumber);
console.log(thirdNumber);
console.log(PI);
console.log(userName);
console.log(itemCount);

